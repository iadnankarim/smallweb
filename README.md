# Smallweb

A small web of one-page sites, and the browser you read them in.

- `web/` — Next.js + Tailwind v4 + shadcn/ui browser
- `api/` — NestJS + MongoDB (to be added)

## Run the web

```bash
cd web
cp .env.example .env.local
npm install
npm run dev
```
