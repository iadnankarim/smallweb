# MongoDB Atlas (or local) connection string, e.g.
# mongodb+srv://user:pass@cluster.mongodb.net/smallweb
MONGODB_URI=

# The web app expects this on http://localhost:4000 by default (see web/.env.example)
PORT=4000
